import { DRAWER_MENU_LABEL } from './drawerMenu';
import { PATH_NAME } from './pathName';
import { USER_ROLE } from './userRole';

export const THEMES = {
  LIGHT: 'light',
  DARK: 'dark',
};

export const LANGUAGE = {
  ENGLISH: 'en',
  VIETNAMESE: 'vn',
};

export { DRAWER_MENU_LABEL, PATH_NAME, USER_ROLE };
